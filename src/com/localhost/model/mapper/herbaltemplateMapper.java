package com.localhost.model.mapper;

import com.localhost.model.po.herbaltemplate;

public interface herbaltemplateMapper {
    int insert(herbaltemplate record);

    int insertSelective(herbaltemplate record);
}