package com.localhost.model.mapper;

import com.localhost.model.po.drugstemplate;

public interface drugstemplateMapper {
    int insert(drugstemplate record);

    int insertSelective(drugstemplate record);
}