package com.localhost.model.mapper;

import com.localhost.model.pojo.settlecategory;

public interface settlecategoryMapper {
    int insert(settlecategory record);

    int insertSelective(settlecategory record);
}