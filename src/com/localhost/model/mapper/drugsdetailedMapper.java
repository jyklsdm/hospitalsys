package com.localhost.model.mapper;

import com.localhost.model.pojo.drugsdetailed;

public interface drugsdetailedMapper {
    int insert(drugsdetailed record);

    int insertSelective(drugsdetailed record);
}