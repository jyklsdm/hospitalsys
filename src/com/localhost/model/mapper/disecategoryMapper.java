package com.localhost.model.mapper;

import com.localhost.model.pojo.disecategory;

public interface disecategoryMapper {
    int insert(disecategory record);

    int insertSelective(disecategory record);
}